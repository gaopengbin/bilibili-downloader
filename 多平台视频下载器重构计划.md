# 多平台视频下载器重构计划
## 一、现状分析
### 代码规模
* `src/App.vue`: 5355 行（所有前端逻辑和 UI）
* `src-tauri/src/lib.rs`: 2437 行（所有后端逻辑）
### 主要问题
1. 所有代码集中在单个文件，难以维护
2. B站特定逻辑与通用逻辑混杂
3. 无法方便地扩展新平台
## 二、目标架构
### 前端结构
```warp-runnable-command
src/
├── main.ts                    # 入口
├── App.vue                    # 根组件（路由、布局）
├── types/                     # 类型定义
│   ├── index.ts               # 通用类型
│   ├── bilibili.ts            # B站特有类型
│   └── youtube.ts             # YouTube特有类型
├── stores/                    # Pinia 状态管理
│   ├── app.ts                 # 全局状态（主题、设置）
│   ├── download.ts            # 下载任务状态
│   └── user.ts                # 用户登录状态
├── composables/               # 组合式函数
│   ├── useDownload.ts         # 下载相关逻辑
│   ├── useTheme.ts            # 主题切换
│   └── useSettings.ts         # 设置管理
├── platforms/                 # 平台适配层
│   ├── base.ts                # 平台基类/接口定义
│   ├── bilibili/
│   │   ├── index.ts           # B站平台实现
│   │   ├── api.ts             # B站 API 调用
│   │   └── types.ts           # B站类型
│   └── youtube/
│       ├── index.ts           # YouTube平台实现
│       └── types.ts           # YouTube类型
├── components/                # 组件
│   ├── common/                # 通用组件
│   │   ├── VideoCard.vue      # 视频卡片
│   │   ├── DownloadCenter.vue # 下载中心
│   │   ├── ProgressBar.vue    # 进度条
│   │   └── QualitySelector.vue# 清晰度选择器
│   ├── layout/                # 布局组件
│   │   ├── Header.vue         # 顶部导航
│   │   ├── Sidebar.vue        # 侧边栏
│   │   └── SplitPanel.vue     # 分栏面板
│   └── bilibili/              # B站专属组件
│       ├── SearchPanel.vue    # 搜索面板
│       ├── HistoryPanel.vue   # 历史记录
│       ├── FavoritesPanel.vue # 收藏夹
│       ├── LoginDialog.vue    # 登录弹窗
│       └── SeasonList.vue     # 合集列表
├── views/                     # 页面视图
│   ├── Home.vue               # 首页（通用下载入口）
│   ├── BilibiliView.vue       # B站专区
│   └── YouTubeView.vue        # YouTube专区
└── styles/                    # 样式
    ├── variables.css          # CSS变量
    ├── common.css             # 通用样式
    └── themes.css             # 主题样式
```
### 后端结构
```warp-runnable-command
src-tauri/src/
├── main.rs                    # 入口
├── lib.rs                     # 命令注册
├── commands/                  # Tauri 命令
│   ├── mod.rs
│   ├── download.rs            # 下载相关命令
│   ├── auth.rs                # 认证相关命令
│   └── video.rs               # 视频信息命令
├── platforms/                 # 平台适配
│   ├── mod.rs
│   ├── common.rs              # 通用下载逻辑（yt-dlp 调用）
│   └── bilibili.rs            # B站特有逻辑（API、合集等）
├── models/                    # 数据模型
│   ├── mod.rs
│   ├── video.rs               # 视频信息
│   └── user.rs                # 用户信息
└── utils/                     # 工具函数
    ├── mod.rs
    └── filename.rs            # 文件名处理
```
## 三、平台接口设计
```typescript
// platforms/base.ts
export interface Platform {
  // 平台标识
  id: string;
  name: string;
  icon: string;
  // 功能支持
  features: {
    search: boolean;      // 是否支持搜索
    login: boolean;       // 是否支持登录
    history: boolean;     // 是否支持历史记录
    favorites: boolean;   // 是否支持收藏
    playlist: boolean;    // 是否支持播放列表
  };
  // 核心方法
  parseUrl(url: string): Promise<VideoInfo>;
  download(url: string, options: DownloadOptions): Promise<void>;
  // 可选方法
  search?(keyword: string, page: number): Promise<SearchResult>;
  login?(): Promise<UserInfo>;
  logout?(): Promise<void>;
  getHistory?(cursor?: number): Promise<HistoryItem[]>;
  getFavorites?(folderId?: number): Promise<FavoriteItem[]>;
}
```
## 四、重构步骤
### 阶段一：基础设施（预计 2-3 小时）
1. 安装 Pinia、Vue Router
2. 创建目录结构
3. 提取类型定义到 `types/`
4. 创建基础样式文件
### 阶段二：状态管理重构（预计 2-3 小时）
1. 创建 `stores/app.ts` - 主题、设置
2. 创建 `stores/download.ts` - 下载任务管理
3. 创建 `stores/user.ts` - 用户状态
4. 迁移 localStorage 逻辑
### 阶段三：组件拆分（预计 3-4 小时）
1. 提取布局组件（Header、Sidebar、SplitPanel）
2. 提取通用组件（VideoCard、DownloadCenter、ProgressBar）
3. 提取 B站专属组件（SearchPanel、LoginDialog 等）
4. 确保组件可独立运行
### 阶段四：平台层抽象（预计 2-3 小时）
1. 定义平台接口 `platforms/base.ts`
2. 实现 B站平台 `platforms/bilibili/`
3. 创建通用下载入口（直接传 URL 给 yt-dlp）
### 阶段五：视图与路由（预计 2-3 小时）
1. 配置 Vue Router
2. 创建首页（通用下载）
3. 创建 B站专区页面
4. 添加导航切换
### 阶段六：后端重构（预计 2-3 小时）
1. 拆分 `lib.rs` 到多个模块
2. 抽象平台无关的下载逻辑
3. 保持 API 兼容性
### 阶段七：测试与优化（预计 1-2 小时）
1. 功能测试（下载、登录、搜索等）
2. UI 测试（主题切换、响应式）
3. 性能优化（懒加载组件）
## 五、优先级排序
高优先级（必须完成）：
* 组件拆分（让代码可维护）
* 平台接口抽象（支持扩展）
* 通用下载入口（验证可行性）
中优先级（建议完成）：
* 状态管理（Pinia）
* 后端模块化
低优先级（可后续完成）：
* 完整的路由系统
* YouTube 专区实现
* 更多平台支持
## 六、风险与注意事项
1. **保持向后兼容**：重构过程中不破坏现有功能
2. **渐进式重构**：每完成一步就能运行，不要大爆炸式重构
3. **Git 分支**：在新分支上进行，方便回滚
4. **测试覆盖**：重点功能（下载、登录）每步都要测试
## 七、时间估算
| 阶段 | 预计时间 |
|------|----------|
| 阶段一：基础设施 | 2-3 小时 |
| 阶段二：状态管理 | 2-3 小时 |
| 阶段三：组件拆分 | 3-4 小时 |
| 阶段四：平台层 | 2-3 小时 |
| 阶段五：视图路由 | 2-3 小时 |
| 阶段六：后端重构 | 2-3 小时 |
| 阶段七：测试优化 | 1-2 小时 |
| **总计** | **14-21 小时** |
建议分 2-3 天完成，每天完成 2-3 个阶段。